Krishna Tissues redesigned static website

Open index.html in a browser, or upload the full folder contents to a static hosting provider.
Keep the assets directory next to the HTML files so videos, images, CSS and JavaScript load correctly.

Notes:
- Source page content and page structure are preserved as header/main/section/footer HTML.
- The contact form uses a mailto action because no backend endpoint was provided.
- Replace relative sitemap URLs with the production domain before deployment.
